package com.payguard.payment.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class StripeConfig {
    // Stripe API key is set in StripeService via @Value
}